# Core framework
fastapi==0.111.0
uvicorn[standard]==0.30.0
pydantic==2.13.4
pydantic-settings==2.3.0
python-dotenv==1.0.1

# LangChain + LangGraph  (fully compatible 1.x set — verified via pip dry-run)
langchain==1.3.9
langchain-core==1.4.7
langchain-openai==1.3.2
langchain-anthropic==1.4.6
langchain-text-splitters==1.1.2   # must be 1.x — 0.2.x pins langchain-core<0.3
langgraph==1.2.5

# MySQL
PyMySQL==1.1.1
SQLAlchemy==2.0.30
aiomysql==0.2.0

# Web search
tavily-python==0.3.3

# Embeddings & semantic similarity
sentence-transformers==3.0.1
numpy==1.26.4

# CLI
typer[all]==0.12.3

# Utilities
httpx==0.28.1        # pulled by anthropic; pinned here for consistency
tenacity==9.1.4      # compatible with langgraph 1.x
structlog==24.2.0
rich==13.7.1

# ─── Bond RAG (PDF pipeline) ──────────────────────────────────────────────────
pymupdf>=1.24.0              # PDF parsing (fitz)
torch>=2.0.0                 # required by sentence-transformers & cross-encoder
sentence-transformers>=3.0.0 # dense embeddings (BAAI/bge-large-en-v1.5) + cross-encoder reranker
chromadb>=0.5.0              # vector store
rank-bm25>=0.2.2             # BM25 sparse retrieval
pytesseract>=0.3.10          # OCR (needs tesseract binary installed separately)
Pillow>=10.0.0               # image processing for OCR
loguru>=0.7.0                # bond_rag internal logging
python-multipart>=0.0.9      # FastAPI file upload support
codecarbon>=2.3.0            # required by sentence-transformers 3.x (CodeCarbonCallback)
# ollama is NOT required — bond_rag's LLM is bypassed; synthesis uses Anthropic/OpenAI
